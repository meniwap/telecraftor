# Group Bot Example

This folder shows the intended external shape for a Telecraft MTProto group bot.

Copy `bot_config.example.json` to an ignored local config file, fill in your group or channel,
and run the internal runner from `apps/group_bot.py` while this example remains small and clean.
